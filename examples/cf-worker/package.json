{
  "name": "worker-typescript-template",
  "type": "module",
  "version": "1.0.0",
  "description": "Cloudflare worker TypeScript template",
  "main": "worker.js",
  "scripts": {
    "build": "esbuild --bundle --outfile=worker.js --format=esm --conditions=node --minify --sourcemap src/index.ts --source-root=/"
  },
  "author": "author",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "typescript": "^5.0.4",
    "esbuild": "0.17.18"
  },
  "dependencies": {
    "@friendsofshopware/app-server-sdk": "^0.0.29",
    "@friendsofshopware/app-server-sdk-cloudflare": "^0.0.29"
  }
}
